'use strict';

// Registrar componente `serie`
angular.
  module('serie').
  component('serie', {
    templateUrl: 'serie/serie.template.html',
    controller: [
      function serieController() {
        var ctrl = this;
    }]
  });
